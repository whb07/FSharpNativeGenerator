namespace FSharpNativeGenerator.Cli

// Minimal stub – real implementation will be completed when
// the loader and host are fully wired in Phases 4-5.
module Program =
    [<EntryPoint>]
    let main _ = 0